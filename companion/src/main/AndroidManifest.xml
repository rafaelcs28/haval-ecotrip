<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Mantém CPU acordada durante o serviço (impede o thread MQTT de hibernar). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Permite pedir ao usuário pra dispensar otimização de bateria — único modo de
         a conexão MQTT sobreviver Doze mode em dispositivo dedicado. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Sobrepor outros apps: no Android 10+ é o que permite o serviço abrir o
         Maps/Waze a partir do BACKGROUND (sem isso o launch é bloqueado silenciosamente). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Pra abrir Maps/Waze via package em targetSdk alto (boa prática). -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
        <package android:name="com.waze" />
    </queries>

    <application
        android:label="Nav Relay"
        android:icon="@android:drawable/ic_menu_mylocation"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Alvo de "Compartilhar" do Maps/Waze: manda o destino pro carro. -->
        <activity
            android:name=".ShareActivity"
            android:exported="true"
            android:label="Enviar pro carro (Ecotrip)"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".NavRelayService"
            android:exported="false" />
    </application>
</manifest>
